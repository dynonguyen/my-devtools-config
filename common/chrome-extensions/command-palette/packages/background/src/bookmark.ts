import { Bookmark } from '@dcp/shared';

// -----------------------------
enum CacheKey {
	FlattedBookmarks = '',
}

const cache = new Cache();

// -----------------------------
const getBookmark = async (): Bookmark => {};

// -----------------------------
